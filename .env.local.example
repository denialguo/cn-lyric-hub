# Supabase project settings → API
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=

# Scripts only. Bypasses row-level security — never commit a real value and
# never give this a VITE_ prefix, or Vite will inline it into the bundle.
SUPABASE_SERVICE_ROLE_KEY=
