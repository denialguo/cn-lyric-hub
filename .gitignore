# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets. .env.local is already covered by *.local, but the scripts fall back
# to a plain .env, which is not — and that file would hold the service-role key.
.env
.env.*
!.env.local.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Agent skill tooling (vendored skills + local symlinks; .mcp.json is kept/shared)
.agents/
.claude/skills/
skills-lock.json

# Personal notes, not project docs
CN_LYRIC_HUB_INTERVIEW_GUIDE.md

# Codex agent config (mirrors .agents/, kept local)
.codex/
